# 2026-08-04

## Gemini Vision configurado ✅

- Van quer mais modelos/agentes (só tinha DeepSeek, texto-only).
- Criei provedor `gemini-2` (OpenAI-compat, generativelanguage.googleapis.com/v1beta/openai/) + auth profile `gemini-2:default` via `openclaw models auth paste-api-key`.
- Chave vision-2 (projeto statsfut-vision-2) em `workspace/.secrets/gemini_keys.json` (600). Chave vision-1 (projeto statsfut-vision-1) chegou às 07:30 e também foi validada — as 2 chaves leem prints.
- **Achado importante:** `gemini-2.5-flash` deu 404 "no longer available to new users" — chaves novas só usam os 3.x. Testei e funcionou com **gemini-3.5-flash** (leu print fake: texto, placar, código). `gemini-3-flash-preview` deu 503 (pico).
- Config: agents.list = [main (SYEL, DeepSeek, default), vision (👁️)]; providers = deepseek, gemini-1, gemini-2 (cada um com 3.5-flash / 3.6-flash / 3-flash-preview, text+image, custo 0).
- **Vision agent:** primary `gemini-1/gemini-3.5-flash`, fallbacks → `gemini-2/gemini-3.5-flash` → `gemini-2/gemini-3.6-flash` (escadinha de 2 cotas grátis).
- Gateway reiniciado 2x (interrompe turnos em andamento — normal).
- Backup config: `openclaw.json.bak.pre-gemini`.
- Decisão de arquitetura com o Van: escadinha de fallback → gemini-1 (quando chegar) → gemini-2 → OpenRouter. Cota grátis é por projeto Google; combinamos máx. 2-3 projetos (risco de ban na conta GSC/Blogger se abusar).
- Custo: 0. DeepSeek segue como cavalo de batalha.
- **BUG CAÇADO (manhã):** tool de imagem falhava `400 no body` no Gemini. Testes diretos na API funcionavam; pelo OpenClaw não. Proxy local (127.0.0.1:18888) capturou o request real → payload continha `"store": false` (compat auto-detectado), que a API Gemini rejeita ("Unknown name store"). Fix: `compat.supportsStore: false` nos 6 modelos gemini-1/gemini-2 + `supportsReasoningEffort: false` no gemini-3-flash-preview. **Print real do canal YouTube lido com sucesso após o fix** (vídeos, inscritos 1,9k, banner).
- Observações: visão do Gemini converte PNG→JPEG (aceito normalmente); rate-limit grátis existe (429 em rajadas — 2 cotas ajudam); debug de payload via env `OPENCLAW_DEBUG_MODEL_PAYLOAD=full-redacted` não aparece no log padrão (usei proxy pra capturar).

## Pendências
- Chave OpenRouter (openrouter.ai) se Van quiser a reserva extra de visão/texto.
- Memory index quebrado (embedding provider mudou) → rodar `openclaw memory index --force` quando possível.

## 07:58 — Troca de credenciais do statsfut2 ✅
- Basic Auth do statsfut2 trocado a pedido do Van: user `userpremium` / senha `importe*$2010` (login do Van; site é só teste dele). Antigo `teste`/`sf29565Xk9` desativado.
- .env editado (BASIC_AUTH_USER/PASS, senha entre aspas simples por causa do `$`), gunicorn reiniciado.
- Testes: 401 sem senha, 200 com userpremium, 401 com credencial antiga. Backup: .env.bak-20260804.
- TOOLS.md atualizado (senha do userpremium agora anotada).

## 08:20 — Ajustes no statsfut2 (feitos por mim, não pelo Gemini) ✅
- **Contexto:** Van pediu ajustes no site de teste; o agente vision (Gemini) ficou só na investigação (grep + leitura), NÃO editou nada (git status limpo). Van desconfiou e pediu pra conferir — confirmei e fiz eu mesmo.
- **1) AdSense/Ezoic desativados no statsfut2:** flag `ADS_ENABLED=False` no `.env` (linha 98) → `core/settings.py` lê env (`ADS_ENABLED`); novo context processor `matches.context_processors.ads_context` registrado em settings; `core/templates/base.html` envolve scripts AdSense + Ezoic + JS de injeção (`Global Ad Injection`) com `{% if ads_enabled %}`. Código continua igual à produção (só o .env difere).
- **2) Cards de jogos finalizados fora da home:** `matches/views.py` — `get_queryset` da HomeView agora faz `.exclude(status__in=['Finished','FT','AET','PEN','FINISHED'])` nos 3 filtros (today/tomorrow/next_round); mesmo filtro no "Palpite do Dia" (tip_of_the_day, ~linha 1119).
- **Cache:** home usa `{% cache 300 home_matches %}` (FileBasedCache /tmp/statsfut_cache) → limpei com `cache.clear()` após deploy.
- **Testes (curl com Basic Auth):** 200 OK; 0 cards finalizados; 0 scripts adsbygoogle/ezoic; 0 ad-placeholder; 6 cards de jogos restantes; palpite do dia ok. `manage.py check` sem issues.
- Backup .env: `.env.bak-20260804` (do troca de senha) ainda existe.

## 08:30 — Tor/SofaScore: endpoint de jogos futuros mudou (fix + sync) ✅
- **Sintoma:** statsfut2 só mostrava 1 liga (Islândia) hoje; banco tinha jogos de amanhã/fim de semana mas não de hoje.
- **Causa raiz:** SofaScore DESCONTINUOU `/sport/football/scheduled-events/{data}` (404 em todos formatos). Novo formato: `/unique-tournament/{id}/season/{sid}/events/next/{pagina}` (200, 30 eventos/página). O `sofascore_tor.py` ainda usava o antigo → Tor não trazia mais jogos novos.
- **Fix em `matches/services/sofascore_tor.py`:** reescrito `get_upcoming_fixtures(days_ahead)` para varrer SOFA_LEAGUE_IDS → seasons → events/next/0..3, filtrando por janela de datas. Ampliado SOFA_LEAGUE_IDS com ligas que faltavam (Colômbia 11539, Peru 406, Chile 11653, Equador Serie B 10240, USL 13363, Eliteserien 20, Allsvenskan 40, Veikkausliiga 41, Besta deild 188, Czech FL 172, 1st Division NO 22, Superettan 46, Ykkösliiga 55, 1. deild IS 675).
- **Hoje (04/08) no SofaScore:** 4 jogos em 3 ligas (Besta deild, Primera A Colômbia ×2, Copa do Brasil) → banco populado via process_fixtures (1 novo + atualizações).
- **Home agora:** 6 cards Scheduled hoje (Islândia 16:15, Colômbia 18:10/20:15/22:20, Copa do Brasil 19:30/21:30), 0 Finished, 0 anúncios.
- **Automação:** criado `/www/wwwroot/statsfut2.statsfut.com/sync_upcoming_tor.sh` (busca 3 dias + processa) + crontab `0 */6 * * *`. Loop live (60s) continua como estava.
- **Pendência:** Bélgica Pro League ainda sem ID (SOFA_LEAGUE_IDS vazio p/ Pro League); `update_live_matches --mode upcoming` via LEAGUE_MAPPINGS pode duplicar nomes (ex: Boca x Estudiantes L.P. vs Estudiantes de La Plata — duplicatas na listagem de amanhã; normalizar depois).

## 09:00 — Heartbeat: resposta de outreach 📬
- **Sports Data Campus respondeu** (03/08 18:17 BRT, David R. Sáez Ávila, CEO): agradeceu a proposta, disse que vão avaliar a ferramenta. Lead vivo, sem compromisso.
- Registrado em `lote1-emails.md` (tabela de tracking). Próximo passo: follow-up curto em ES só após ~1-2 semanas e com OK do Van. Não responder agora.

## 13:30 — Todas as ligas alimentadas via Tor (mapa completo) ✅
- **Pedido do Van:** "todas elas funcionem, não faz sentido ter ligas sem alimentação".
- **Problema:** SOFA_LEAGUE_IDS tinha só ~20 ligas; league_map tinha nomes errados (mapeava p/ ligas que não existiam no banco, criando duplicatas 85/86/87/88); sync buscava só 3 dias.
- **Feito:**
  - Mapeei TODOS os IDs SofaScore das 53 ligas do banco (validados via API): MLS=242, Championship=18, Premiership=36, J1=196, Liga MX=11621, Ekstraklasa=202, Pro League=38, Paraguai=11541 (era 11540 Apertura), Uruguai=30743 (era 18000 sem seasons), Chile B=1240, Argentina B=703, Libertadores=16940, Sul-Americana=480, etc.
  - league_map completo no update_live_matches.py com TODOS os nomes atuais do SofaScore (Brasileirão Betano, LaLiga, Liga Portugal Betclic, VriendenLoterij Eredivisie, Trendyol Süper Lig, Danish Superliga, Swiss/Stoiximan Super League, Primera División c/ acento, etc.)
  - sync_upcoming_tor.sh: janela 3 → 45 dias (cobre pré-temporada europeia: Bundesliga 28/08, Ligue 1/PL 21/08, Serie A 22/08).
  - Corrigi duplicatas do banco: migrei jogos de 85(Serie A)→2(Brasileirão), 86→70(Série B), 87→71(Série C), corrigi times p/ canônicos (10 fix, 14 dups deletadas), removi ligas 85/86/87/88.
- **Resultado:** 48/49 ligas com jogos nos próximos 45 dias (única sem: A-League, volta outubro). Home: 18 cards hoje, 0 finalizados, 0 anúncios, 30 países no sidebar.
- **Cron:** `0 */6 * * * sync_upcoming_tor.sh` (45 dias, ~12 min via Tor, 2290 fixtures).

## 13:40 — Home do statsfut2: 6 cards HOJE é o correto ✅
- Van reclamou "só vejo 7 cards". Investigação: o SofaScore REALMENTE tem só 6 jogos hoje (04/08, terça): Islândia 15:30, Colômbia ×3 (18:10/20:15/22:20), Copa do Brasil ×2 (19:30/21:30). Terça é dia fraco + ligas europeias em pré-temporada.
- O 7º card era duplicata (Thor x Breidablik com api_id antigo 1508822 da API-Football vs novo 15426887 do SofaScore) → removida.
- Amanhã (05/08) tem 28 cards: Argentina (Liga Profesional + Primera B), Copa do Brasil, Colômbia, Equador Série B, USL Championship, Islândia. Fim de semana 100+.
- Confirmado: home HOJE = 6 cards Scheduled = correto (não é bug).

## 19:40 — OpenRouter configurado como provedor reserva ✅
- Van tem conta OpenRouter (chave antiga sk-or-…5446, **ativa**, `is_free_tier: false`, sem limite — tem créditos).
- Chave salva em `.secrets/openrouter_key` (600). Auth profile `openrouter:default` criado via `openclaw models auth paste-api-key`.
- Provider `openrouter` adicionado no openclaw.json (baseUrl https://openrouter.ai/api/v1) com 4 modelos:
  - `deepseek/deepseek-chat` ($0.26/M in, $1.03/M out) — principal reserva
  - `deepseek/deepseek-r1` (reasoning, $0.70/$2.50)
  - `meta-llama/llama-3.3-70b-instruct` ($0.10/$0.32) — teste ok ✅
  - `qwen/qwen-2.5-72b-instruct` ($0.36/$0.40)
- compat `supportsStore: false` aplicado (mesmo fix do Gemini — API OpenRouter rejeita o campo `store`).
- Gateway reiniciado, config validada, modelos listados. Teste real via Llama 70B: "OpenRouter funcionando via Llama 70B" ✅.
- Backup config: `openclaw.json.bak-pre-openrouter`.
- Uso: `/model openrouter/deepseek/deepseek-chat` no chat. Custo real mínimo (chave tem créditos, mas modelos baratos).
- ⚠️ Conexão caiu no meio (19:32-19:40) — gateway reiniciou normal, nada quebrado.

## 20:00 — OpenRouter completo: 9 modelos (texto + visão) ✅
- Adicionados 5 modelos novos ao provider openrouter (total 9): 
  - **Visão:** `qwen/qwen3.7-flash` (text+image, $0.03/$0.13, 1M ctx) + `openai/gpt-5-nano` (text+image, $0.05/$0.40)
  - **Texto/código:** `qwen/qwen3-coder-30b-a3b-instruct` ($0.07/$0.27), `openai/gpt-oss-120b` ($0.037/$0.17), `qwen/qwen3-32b` ($0.08/$0.28)
- Todos com `compat.supportsStore: false` (fix obrigatório p/ OpenRouter, igual Gemini).
- **Teste de visão REAL:** qwen3.7-flash leu o print do statsfut2 (jogos Islândia, placares, % O2.5/U1.5, banner) ✅. Modelos de reasoning respondem no campo `reasoning` (content null) — normal.
- Gateway reiniciado, config validada, 9 modelos listados (2 text+image).
- Backup: openclaw.json.bak-pre-modelos.
- Decisão do Van: **usar modelos pagos** (tem saldo), não os :free (rate-limit/fila).
# 2026-08-04

## Gemini Vision configurado ✅

- Van quer mais modelos/agentes (só tinha DeepSeek, texto-only).
- Criei provedor `gemini-2` (OpenAI-compat, generativelanguage.googleapis.com/v1beta/openai/) + auth profile `gemini-2:default` via `openclaw models auth paste-api-key`.
- Chave vision-2 (projeto statsfut-vision-2) em `workspace/.secrets/gemini_keys.json` (600). Chave vision-1 (projeto statsfut-vision-1) chegou às 07:30 e também foi validada — as 2 chaves leem prints.
- **Achado importante:** `gemini-2.5-flash` deu 404 "no longer available to new users" — chaves novas só usam os 3.x. Testei e funcionou com **gemini-3.5-flash** (leu print fake: texto, placar, código). `gemini-3-flash-preview` deu 503 (pico).
- Config: agents.list = [main (SYEL, DeepSeek, default), vision (👁️)]; providers = deepseek, gemini-1, gemini-2 (cada um com 3.5-flash / 3.6-flash / 3-flash-preview, text+image, custo 0).
- **Vision agent:** primary `gemini-1/gemini-3.5-flash`, fallbacks → `gemini-2/gemini-3.5-flash` → `gemini-2/gemini-3.6-flash` (escadinha de 2 cotas grátis).
- Gateway reiniciado 2x (interrompe turnos em andamento — normal).
- Backup config: `openclaw.json.bak.pre-gemini`.
- Decisão de arquitetura com o Van: escadinha de fallback → gemini-1 (quando chegar) → gemini-2 → OpenRouter. Cota grátis é por projeto Google; combinamos máx. 2-3 projetos (risco de ban na conta GSC/Blogger se abusar).
- Custo: 0. DeepSeek segue como cavalo de batalha.
- **BUG CAÇADO (manhã):** tool de imagem falhava `400 no body` no Gemini. Testes diretos na API funcionavam; pelo OpenClaw não. Proxy local (127.0.0.1:18888) capturou o request real → payload continha `"store": false` (compat auto-detectado), que a API Gemini rejeita ("Unknown name store"). Fix: `compat.supportsStore: false` nos 6 modelos gemini-1/gemini-2 + `supportsReasoningEffort: false` no gemini-3-flash-preview. **Print real do canal YouTube lido com sucesso após o fix** (vídeos, inscritos 1,9k, banner).
- Observações: visão do Gemini converte PNG→JPEG (aceito normalmente); rate-limit grátis existe (429 em rajadas — 2 cotas ajudam); debug de payload via env `OPENCLAW_DEBUG_MODEL_PAYLOAD=full-redacted` não aparece no log padrão (usei proxy pra capturar).

## Pendências
- Chave OpenRouter (openrouter.ai) se Van quiser a reserva extra de visão/texto.
- Memory index quebrado (embedding provider mudou) → rodar `openclaw memory index --force` quando possível.

## 07:58 — Troca de credenciais do statsfut2 ✅
- Basic Auth do statsfut2 trocado a pedido do Van: user `userpremium` / senha `importe*$2010` (login do Van; site é só teste dele). Antigo `teste`/`sf29565Xk9` desativado.
- .env editado (BASIC_AUTH_USER/PASS, senha entre aspas simples por causa do `$`), gunicorn reiniciado.
- Testes: 401 sem senha, 200 com userpremium, 401 com credencial antiga. Backup: .env.bak-20260804.
- TOOLS.md atualizado (senha do userpremium agora anotada).

## 08:20 — Ajustes no statsfut2 (feitos por mim, não pelo Gemini) ✅
- **Contexto:** Van pediu ajustes no site de teste; o agente vision (Gemini) ficou só na investigação (grep + leitura), NÃO editou nada (git status limpo). Van desconfiou e pediu pra conferir — confirmei e fiz eu mesmo.
- **1) AdSense/Ezoic desativados no statsfut2:** flag `ADS_ENABLED=False` no `.env` (linha 98) → `core/settings.py` lê env (`ADS_ENABLED`); novo context processor `matches.context_processors.ads_context` registrado em settings; `core/templates/base.html` envolve scripts AdSense + Ezoic + JS de injeção (`Global Ad Injection`) com `{% if ads_enabled %}`. Código continua igual à produção (só o .env difere).
- **2) Cards de jogos finalizados fora da home:** `matches/views.py` — `get_queryset` da HomeView agora faz `.exclude(status__in=['Finished','FT','AET','PEN','FINISHED'])` nos 3 filtros (today/tomorrow/next_round); mesmo filtro no "Palpite do Dia" (tip_of_the_day, ~linha 1119).
- **Cache:** home usa `{% cache 300 home_matches %}` (FileBasedCache /tmp/statsfut_cache) → limpei com `cache.clear()` após deploy.
- **Testes (curl com Basic Auth):** 200 OK; 0 cards finalizados; 0 scripts adsbygoogle/ezoic; 0 ad-placeholder; 6 cards de jogos restantes; palpite do dia ok. `manage.py check` sem issues.
- Backup .env: `.env.bak-20260804` (do troca de senha) ainda existe.

## 08:30 — Tor/SofaScore: endpoint de jogos futuros mudou (fix + sync) ✅
- **Sintoma:** statsfut2 só mostrava 1 liga (Islândia) hoje; banco tinha jogos de amanhã/fim de semana mas não de hoje.
- **Causa raiz:** SofaScore DESCONTINUOU `/sport/football/scheduled-events/{data}` (404 em todos formatos). Novo formato: `/unique-tournament/{id}/season/{sid}/events/next/{pagina}` (200, 30 eventos/página). O `sofascore_tor.py` ainda usava o antigo → Tor não trazia mais jogos novos.
- **Fix em `matches/services/sofascore_tor.py`:** reescrito `get_upcoming_fixtures(days_ahead)` para varrer SOFA_LEAGUE_IDS → seasons → events/next/0..3, filtrando por janela de datas. Ampliado SOFA_LEAGUE_IDS com ligas que faltavam (Colômbia 11539, Peru 406, Chile 11653, Equador Serie B 10240, USL 13363, Eliteserien 20, Allsvenskan 40, Veikkausliiga 41, Besta deild 188, Czech FL 172, 1st Division NO 22, Superettan 46, Ykkösliiga 55, 1. deild IS 675).
- **Hoje (04/08) no SofaScore:** 4 jogos em 3 ligas (Besta deild, Primera A Colômbia ×2, Copa do Brasil) → banco populado via process_fixtures (1 novo + atualizações).
- **Home agora:** 6 cards Scheduled hoje (Islândia 16:15, Colômbia 18:10/20:15/22:20, Copa do Brasil 19:30/21:30), 0 Finished, 0 anúncios.
- **Automação:** criado `/www/wwwroot/statsfut2.statsfut.com/sync_upcoming_tor.sh` (busca 3 dias + processa) + crontab `0 */6 * * *`. Loop live (60s) continua como estava.
- **Pendência:** Bélgica Pro League ainda sem ID (SOFA_LEAGUE_IDS vazio p/ Pro League); `update_live_matches --mode upcoming` via LEAGUE_MAPPINGS pode duplicar nomes (ex: Boca x Estudiantes L.P. vs Estudiantes de La Plata — duplicatas na listagem de amanhã; normalizar depois).

## 09:00 — Heartbeat: resposta de outreach 📬
- **Sports Data Campus respondeu** (03/08 18:17 BRT, David R. Sáez Ávila, CEO): agradeceu a proposta, disse que vão avaliar a ferramenta. Lead vivo, sem compromisso.
- Registrado em `lote1-emails.md` (tabela de tracking). Próximo passo: follow-up curto em ES só após ~1-2 semanas e com OK do Van. Não responder agora.

## 13:30 — Todas as ligas alimentadas via Tor (mapa completo) ✅
- **Pedido do Van:** "todas elas funcionem, não faz sentido ter ligas sem alimentação".
- **Problema:** SOFA_LEAGUE_IDS tinha só ~20 ligas; league_map tinha nomes errados (mapeava p/ ligas que não existiam no banco, criando duplicatas 85/86/87/88); sync buscava só 3 dias.
- **Feito:**
  - Mapeei TODOS os IDs SofaScore das 53 ligas do banco (validados via API): MLS=242, Championship=18, Premiership=36, J1=196, Liga MX=11621, Ekstraklasa=202, Pro League=38, Paraguai=11541 (era 11540 Apertura), Uruguai=30743 (era 18000 sem seasons), Chile B=1240, Argentina B=703, Libertadores=16940, Sul-Americana=480, etc.
  - league_map completo no update_live_matches.py com TODOS os nomes atuais do SofaScore (Brasileirão Betano, LaLiga, Liga Portugal Betclic, VriendenLoterij Eredivisie, Trendyol Süper Lig, Danish Superliga, Swiss/Stoiximan Super League, Primera División c/ acento, etc.)
  - sync_upcoming_tor.sh: janela 3 → 45 dias (cobre pré-temporada europeia: Bundesliga 28/08, Ligue 1/PL 21/08, Serie A 22/08).
  - Corrigi duplicatas do banco: migrei jogos de 85(Serie A)→2(Brasileirão), 86→70(Série B), 87→71(Série C), corrigi times p/ canônicos (10 fix, 14 dups deletadas), removi ligas 85/86/87/88.
- **Resultado:** 48/49 ligas com jogos nos próximos 45 dias (única sem: A-League, volta outubro). Home: 18 cards hoje, 0 finalizados, 0 anúncios, 30 países no sidebar.
- **Cron:** `0 */6 * * * sync_upcoming_tor.sh` (45 dias, ~12 min via Tor, 2290 fixtures).

## 13:40 — Home do statsfut2: 6 cards HOJE é o correto ✅
- Van reclamou "só vejo 7 cards". Investigação: o SofaScore REALMENTE tem só 6 jogos hoje (04/08, terça): Islândia 15:30, Colômbia ×3 (18:10/20:15/22:20), Copa do Brasil ×2 (19:30/21:30). Terça é dia fraco + ligas europeias em pré-temporada.
- O 7º card era duplicata (Thor x Breidablik com api_id antigo 1508822 da API-Football vs novo 15426887 do SofaScore) → removida.
- Amanhã (05/08) tem 28 cards: Argentina (Liga Profesional + Primera B), Copa do Brasil, Colômbia, Equador Série B, USL Championship, Islândia. Fim de semana 100+.
- Confirmado: home HOJE = 6 cards Scheduled = correto (não é bug).

## 19:40 — OpenRouter configurado como provedor reserva ✅
- Van tem conta OpenRouter (chave antiga sk-or-…5446, **ativa**, `is_free_tier: false`, sem limite — tem créditos).
- Chave salva em `.secrets/openrouter_key` (600). Auth profile `openrouter:default` criado via `openclaw models auth paste-api-key`.
- Provider `openrouter` adicionado no openclaw.json (baseUrl https://openrouter.ai/api/v1) com 4 modelos:
  - `deepseek/deepseek-chat` ($0.26/M in, $1.03/M out) — principal reserva
  - `deepseek/deepseek-r1` (reasoning, $0.70/$2.50)
  - `meta-llama/llama-3.3-70b-instruct` ($0.10/$0.32) — teste ok ✅
  - `qwen/qwen-2.5-72b-instruct` ($0.36/$0.40)
- compat `supportsStore: false` aplicado (mesmo fix do Gemini — API OpenRouter rejeita o campo `store`).
- Gateway reiniciado, config validada, modelos listados. Teste real via Llama 70B: "OpenRouter funcionando via Llama 70B" ✅.
- Backup config: `openclaw.json.bak-pre-openrouter`.
- Uso: `/model openrouter/deepseek/deepseek-chat` no chat. Custo real mínimo (chave tem créditos, mas modelos baratos).
- ⚠️ Conexão caiu no meio (19:32-19:40) — gateway reiniciou normal, nada quebrado.

## 20:00 — OpenRouter completo: 9 modelos (texto + visão) ✅
- Adicionados 5 modelos novos ao provider openrouter (total 9): 
  - **Visão:** `qwen/qwen3.7-flash` (text+image, $0.03/$0.13, 1M ctx) + `openai/gpt-5-nano` (text+image, $0.05/$0.40)
  - **Texto/código:** `qwen/qwen3-coder-30b-a3b-instruct` ($0.07/$0.27), `openai/gpt-oss-120b` ($0.037/$0.17), `qwen/qwen3-32b` ($0.08/$0.28)
- Todos com `compat.supportsStore: false` (fix obrigatório p/ OpenRouter, igual Gemini).
- **Teste de visão REAL:** qwen3.7-flash leu o print do statsfut2 (jogos Islândia, placares, % O2.5/U1.5, banner) ✅. Modelos de reasoning respondem no campo `reasoning` (content null) — normal.
- Gateway reiniciado, config validada, 9 modelos listados (2 text+image).
- Backup: openclaw.json.bak-pre-modelos.
- Decisão do Van: **usar modelos pagos** (tem saldo), não os :free (rate-limit/fila).

## 21:00 — /model não listava modelos + latência investigada ✅
- **Bug `/model`:** Van digitava `/model` e via só o modelo atual, sem lista. Causa: o comando lista os modelos em `agents.defaults.models` (a config global) + agent models.json — o OpenRouter não estava lá (só deepseek + gemini-2/gemini-3.5-flash = 2 "configured models"). Os 9 do provider existiam mas não apareciam no menu.
- **Fix:** adicionei os 9 modelos do OpenRouter em `agents.defaults.models` com aliases amigáveis (DeepSeek (OR), DeepSeek R1 (OR), Llama 70B (OR), Qwen 72B (OR), Qwen 3.7 Flash (visão), GPT-5 Nano (visão), Qwen3 Coder, GPT-OSS 120B, Qwen3 32B). Agora 11 modelos configurados. Gateway reiniciado.
- **UX do /model no webchat:** não tem menu clicável — é texto. Trocar = digitar `/model <id>` ou `/model "Alias"` (ex: `/model "Qwen3 Coder"`). Autocomplete funciona digitando parte do nome após o espaço.
- **Reclamação de lentidão (Van):** Qwen/GPT demoraram 10-15s pra responder "oi", mesmo com saldo. Investigação:
  - API direta do OpenRouter é RÁPIDA (qwen3.7-flash 1.2-1.7s, gpt-oss-120b 3.1s com payload pequeno).
  - **Causa raiz real:** a sessão tinha **305k tokens de contexto** (logs do SofaScore etc. de hoje) e o **OpenRouter NÃO tem prompt cache** (cache=0 no log) → reprocessa os 305k do zero a cada mensagem → 10-15s. O **DeepSeek direto tem cache** (~280k cacheRead) → rápido mesmo com conversa gigante.
  - Prova: com contexto pequeno (22k tokens) o Llama respondeu rápido; teste de API com payload mínimo = 1-3s.
  - Modelos reasoning (deepseek-r1, qwen3.7-flash) são mais lentos por natureza (pensam antes) — R1 mediu 10.8s.
- **Decisão:** manter DeepSeek como padrão (rápido + cache); OpenRouter só como reserva/uso pontual com contexto pequeno; não usar R1 no dia a dia. Sugerido ao Van: compactar/limpar conversas gigantes (305k tokens deixa tudo lento, até o DeepSeek).
- **Pergunta do Van (contratar direto sem OpenRouter):** DeepSeek **já é direto** (platform.deepseek.com, é o padrão atual); Qwen dá pra usar direto via Alibaba DashScope; GPT-OSS/GPT-5-nano via platform.openai.com (cartão); Llama **não tem API própria** (Meta não vende — precisa de terceiro). Conclusão passada: a demora não era do OpenRouter, e contratar direto não mudaria nada — o problema era contexto gigante + sem cache.

## Ações pendentes (para próximas sessões)
- [ ] Se Van confirmar: remover deepseek-r1 e qwen-2.5-72b da lista do /model (evitar troca acidental p/ modelos lentos).
- [ ] Oferecer compactação de conversa quando contexto passar de ~150-200k tokens.
- [ ] Monitorar consumo de créditos OpenRouter (openrouter.ai/activity) — custos são centavos.
- [ ] Backups de config do dia: openclaw.json.bak.pre-gemini, .bak-pre-openrouter, .bak-pre-modelos.

## 21:05 — BUG FIXADO: cards ao vivo não apareciam na home (Duplicate entry) ✅
- **Sintoma:** Van reportou que jogos ao vivo não apareciam nos cards da home, mesmo com Tor raspando direitinho.
- **Causa raiz:** loop live rodava OK mas TODA execução falhava com `(1062, "Duplicate entry '1980' for key 'matches_team.api_id'")`. O `Team.api_id` é `unique=True` GLOBAL (não por liga), e o mesmo clube existe em várias ligas (ex: Juventude com api_id='1980' na Série B id=5290, e Juventude com api_id=None na Copa do Brasil id=14047).
- **Fluxo do bug:** SofaScore manda `home_team_id=1980` (id do Juventude). `_get_or_create_team` no `update_live_matches.py`: (1) busca por api_id na liga → não acha na Copa; (2) acha por NOME → vê `api_id=None` → seta `team.api_id='1980'` SEM checar se já existe em outra liga → colide → Duplicate entry → fixture nunca processado → jogo nunca vira "Live" no banco.
- **Fix:** em `update_live_matches.py` `_get_or_create_team` passo 2, adicionei a mesma proteção que o passo 3 já tinha: só seta `api_id` se `not Team.objects.filter(api_id=str(external_id)).exists()` (checagem global, não por liga). Deixa None e o `logo_url` faz fallback por nome.
- **Resultado (testado):** `update_live_matches --mode live --force` → `🔄 Atualizado: Juventude 0-0 Atletico Mineiro (30')` — jogo agora tem status **Live** no banco (id=569327, api_id=16240963 SofaScore). Home mostra 6 cards hoje com o jogo ao vivo no topo (ordenado por status), HTML real com indicador "Live" ✅. Cache limpo (home usa `{% cache 300 %}`).
- **Lição:** api_id unique global + mesmo clube em várias ligas = colisão. Qualquer código que seta api_id precisa checar globalmente, não só na liga.
- ⚠️ Loop live (60s) continua rodando — agora processa sem erro. Próximo sync upcoming (cron 6h) também usa o mesmo método → fix vale pros dois.

## 21:15 — Minuto do jogo corrigido + estatísticas ao vivo no card da home ✅
- **Problema 2 (minuto):** card mostrava 33' mas jogo real estava ~76' (2º tempo). Causa: `_normalize` do sofascore_tor.py calculava elapsed com `currentPeriodStartTimestamp` (início do PERÍODO atual) em vez do `startTimestamp` (início do JOGO) → mostrava minuto do 2º tempo, não do jogo.
- **Fix:** elapsed = (now - startTimestamp - intervalo)/60, onde intervalo = `currentPeriodStartTimestamp - lastPeriodEndTimestamp` (tempo real parado, fallback 15min). Testado: banco passou de 33' → 84' (bate com 19:30 + 99min − 15min intervalo).
- **Observação do Van (válida):** cálculo por relógio é frágil (atraso, acréscimos de 10min, jogo parado). O SofaScore NÃO manda minuto pronto — só timestamps (`startTimestamp`, `currentPeriodStartTimestamp`, `lastPeriodEndTimestamp`, `injuryTime1`, `initial=2700s`, `max=5400s`, `extra=540s`). Aceitei a limitação: relógio é o que a API oferece; o site do SofaScore faz o mesmo cálculo. Se quiser minuto exato no futuro, precisaria de WebSocket (fora do escopo Tor/REST).
- **Problema 3 (estatísticas no card):** Van viu o Google mostrando chutes/escanteios/posse minuto a minuto e perguntou se dá pra raspar do Google. **Resposta: não precisa!** O SofaScore JÁ fornece tudo (`get_match_rich_data`: Total shots, Shots on/off target, Corner kicks, Ball possession, Fouls, graph de pressão) e o loop live já salvava no banco (home_shots=11, away_shots=13, corners 3x4, posse 34x66...). O que faltava era EXIBIR na home.
- **Fix:** `matches/templates/matches/home.html` — adicionado bloco `{% if match.status == 'Live' %}` com `live-stats-row` mostrando Chutes (⚽), Chutes no gol (🎯), Escanteios (🚩), Posse (🔄) — só em jogos ao vivo, não polui cards agendados. Gunicorn reiniciado (workers de ontem não recarregavam template).
- **Resultado:** card ao vivo da home = minuto total correto + 4 estatísticas em tempo real, atualizado a cada 60s. Google é desnecessário (ele compra dados de agregadores como o SofaScore).
- Backup do template: (home.html editado direto; git do projeto preserva histórico).

## 21:25 — Minuto no padrão SofaScore/Google + cache desativado p/ jogos ao vivo ✅
- **Problema:** Van comparou com o Google — nosso minuto (81') não batia (Google: 76'). E o card não mostrava estatísticas (cache de 5min servindo versão velha).
- **Descoberta (crucial):** o SofaScore NÃO manda minuto pronto; o site calcula assim: `minuto = (now - currentPeriodStartTimestamp do período atual)/60 + base`, onde base = 0 (1º tempo, code=6), 45 (2º tempo, code=7/8), 90 (prorrogação, codes 10-13). **NÃO usa startTimestamp** (errado se jogo atrasar!). Códigos SofaScore: 6=1st half, 7=2nd half, 8=Half time, 10=ET break, 11=ET 1st, 13=ET 2nd, 100=FT.
- **Fix em sofascore_tor.py `_normalize`:** elapsed = base + minuto do período atual (antes: now - startTimestamp - intervalo → superestimava em ~5min).
- **Fix cache home:** `{% cache %}` não pode ser envolvido por `{% if %}` (KeyError 'endif' — a tag captura até endcache). Solução: `{% cache cache_timeout ... %}` com `context['cache_timeout'] = 0 if has_live_matches else 300` na HomeView.get_context_data. Com jogo ao vivo → SEM cache (regenera a cada request); sem live → cache 5min normal.
- **Testado:** HTTP 200, card com 91' (bate com relógio do jogo), chutes 12x14, no gol 3x2, escanteios 3x4, posse 34%. cache_timeout=0 confirmado via shell.
- ⚠️ Lição: tag cache do Django não aninha com if; usar timeout dinâmico.

## 21:35 — Radar Ao Vivo + sidebar H2H turbinados ✅
- **Pedido do Van:** popup "Radar Ao Vivo" não mostrava Atq. Perigosos (sempre 0x0) e queria upgrade no popup + no box da sidebar da página H2H.
- **Descoberta:** o campo `home_dangerous_attacks` existia no model mas NUNCA era preenchido. O SofaScore NÃO expõe "Dangerous attacks" na API REST — o proxy oficial mais próximo é **"Final third entries"** (entradas no terço final). Big chances também não era coletado.
- **Feito:**
  - `sofascore_tor.py get_match_rich_data`: + `home/away_dangerous_attacks` (Final third entries) e `home/away_big_chances` (Big chances).
  - `matches/models.py`: + campos `home_big_chances`/`away_big_chances` → migration `0046_add_big_chances`.
  - `update_live_matches.py`: salva os 4 campos novos no rich data.
  - `live_radar_modal.html`: grid 2 linhas × 3 colunas — Chutes no Alvo, Chutes, Grandes Chances / Escanteios, Atq. Perigosos, Faltas (antes: só 3 métricas).
  - `h2h_detail.html` sidebar: + No Alvo, Posse, Atq. Perig., Grd. Chanc. (antes: só escanteios + chutes).
- **Resultado (testado):** Juventude x Atlético-MG → atq_perig=36x70, grd_chanc=2x2, chutes=15x15, no_alvo=3x3, faltas=18x12. H2H HTTP 200 com todos os campos no HTML. Popup via AJAX (`live_radar_partial`) usa o template novo sem cache.
- **Obs:** Final third entries (36x70) é um número maior que "ataques perigosos" do FlashScore (~90x110) mas mede o mesmo conceito; aceitável como proxy.

## 21:45 — Loop live agora processa TODOS os jogos ao vivo (fix do filtro por nome) ✅
- **Pedido do Van:** "não importa quantos jogos ao vivo, tem que raspar todos" (viu Deportes Tolima x Independiente Medellín no Google ao vivo, mas nossa home não mostrava).
- **Causa raiz (dupla):**
  1. `enabled_leagues` no update_live_matches era uma lista RESTRITIVA de ~20 ligas (faltava Colômbia, Chile, Peru, México, MLS etc.).
  2. O filtro usava `SOFA_IDS.get(lg['name'])` — comparava o NOME que o SofaScore manda (ex: "Copa Betano do Brasil") com a chave do mapa ("Copa do Brasil") → retornava [] e ignorava o jogo. Remo x Santos (Copa do Brasil, ao vivo!) era ignorado por isso.
- **Fix:** filtro reescrito para usar **ID de torneio** (conjunto achatado dos 49 IDs do SOFA_LEAGUE_IDS) em vez de nome. `enabled_leagues` agora é derivado do SOFA_LEAGUE_IDS (43 ligas). process_fixtures com readonly_structure=True (não cria duplicatas).
- **Resultado:** `⚡ 2 jogos AO VIVO nas ligas monitoradas` → Remo x Santos (0-0) e Deportes Tolima x Ind. Medellín (1-3, 59') processados com rich data (escanteios 4x2, chutes 7x11, posse 61x39). Home mostra os 2 cards LIVE (HTTP 200 confirmado).
- **Comportamento fim de jogo confirmado:** Juventude x Atlético-MG (acabou 0-1) saiu do radar do SofaScore → marcado FT pela lógica stale (>2h) → card sumiu da home. ✅ (Van pediu exatamente isso: "quando não mostrar mais ao vivo no SofaScore, não precisa mostrar o card").
- **Nota:** rich data do Remo x Santos veio 0x0 (jogo com 1', estatísticas ainda não populares). Graph 404 em jogo recém-começado é normal (tratado).
- **Lição:** filtrar por ID estável > nome (SofaScore renomeia ligas por patrocínio: "Copa Betano do Brasil", "Brasileirão Betano", "Trendyol Süper Lig" etc.).

## 21:50 — Sem limite de jogos ao vivo + esclarecimento das 49 ligas ✅
- **Pergunta do Van:** por que 43? Ele tem ~47-49 ligas. E se tiver 30 jogos ao vivo, mostra todos ou limita?
- **Esclarecimento:** banco tem 49 ligas; SOFA_LEAGUE_IDS tem 43 CHAVES mas TODAS as 49 ligas têm ID (algumas chaves cobrem 2 ligas: First League=Tcheca, Bundesliga=Alemanha+Áustria, Primera Division=Chile+Paraguai+Uruguai, Premier League=Inglaterra+Ucrânia). Extras: chave "Ukrainian Premier League" sem liga correspondente no banco (ok).
- **Limite encontrado e REMOVIDO:** rich data (chutes/escanteios/posse) tinha `[:5]` (só 5 jogos/ciclo recebiam estatísticas). Placar/minuto ao vivo nunca teve limite. Agora rich data = TODOS os jogos ao vivo (sem [:5]).
- **Testado:** 2 jogos ao vivo (Remo x Santos 4', Tolima 1-3 63') com rich data completo. Custo Tor: ~2 requests/jogo/ciclo, rotaciona circuito — 30 jogos = ~60 requests, viável.
- **Resposta ao Van:** sim, mostra TODOS — placar/minuto sempre; estatísticas ricas agora também sem limite.

## 21:55 — Índice de Pressão do Radar redesenhado (visual) ✅
- **Pedido do Van:** deixar o "Índice de Pressão (Radar)" mais elegante/chamativo; perguntou se os cálculos estão corretos.
- **Cálculo = correto:** usa o gráfico de pressão OFICIAL do SofaScore (graph_points em statistics_data) — soma momentum dos últimos N min (5/10/15) e jogo todo. Fallback: snapshots (chutes×3 + escanteios×2 + atq.perigosos×1). Status: >60% = Dominando, senão Equilibrado. Ex: Tolima 100% Casa nos 5min = perdendo 1-3 e atacando no fim (correto).
- **Novo visual (live_radar_modal.html):** loop `pressure_windows` na view (lista de tuplas label+dict); cada janela = card com badge de status (🔥 Casa Dominando verde / 🔥 Fora Dominando vermelho / ⚖️ Equilibrado amarelo) + barra de duelo com gradiente verde↔vermelho, divisória central branca, % com rótulos CASA/FORA, animação 1s.
- Testado: modal HTTP 200 com 4 pressure-blocks, barras 100%/72%/28% etc.

## 22:00 — Saneamento automático de jogos "Scheduled" com hora passada ✅
- **Problema do Van:** jogos de hoje (Islândia 15:30, Colômbia 18:10) listados como Scheduled SEM resultado na home — e ele não quer ficar olhando manualmente.
- **Causa:** o loop live só pega jogos EM ANDAMENTO no radar do SofaScore. Se o jogo começa e termina entre ciclos (ou Tor falha naquele momento), fica Scheduled pra sempre. Havia 897 jogos Scheduled com hora passada no banco (a maioria antigos com api_id='ignored_XXX' da API-Football — lixo histórico).
- **Fix:**
  1. `sofascore_tor.py`: novo método `get_event_result(event_id)` — busca `/event/{id}`, retorna status (FT/LIVE/NS/PST/CANC/ABD/TBD) + placar.
  2. `update_live_matches.py` (modo live): bloco de SANEAMENTO após rich data — pega Scheduled com data >2h atrás, **prioriza IDs de 8+ dígitos** (SofaScore; IDs de 7 dígitos da API-Football dão 404 no SofaScore), até 10/ciclo, 1 request/jogo via Tor: FT → grava placar+status; PST/CANC/ABD/TBD → marca correspondente. Import adicionado: `random`, `time`.
- **Testado:** Thor Akureyri 1-0 Breidablik (era 15:30 Scheduled) e Llaneros 3-1 Fortaleza corrigidos → FT. Home limpa (0 menções), só os 2 ao vivo (Remo x Santos, Tolima 1-3 Medellín 81').
- **Obs:** os ~897 jogos com api_id='ignored_*' são históricos antigos sem resultado — não são tocados (sem ID válido p/ consultar). Se Van quiser, dá pra limpar/arquivar num momento futuro (cuidado: alguns podem ser de temporadas antigas ainda exibidos em páginas de liga).
