# 2026-08-01

- Bootstrap concluído: me chamo **SYEL** ⚡; humano é o **Van** (PT-BR, fuso America/Sao_Paulo).
- Van quer turbinar o site dele: performance, SEO, cadastros em diretórios e backlinks legítimos.
- Regra: ações externas (e-mail, publicação, cadastro) só com OK do Van.
- Site do Van: **statsfut.com** (estatísticas/palpites de futebol, multilíngue: EN padrão + PT-BR, ES, DE com hreflang correto). Stack Cloudflare+LiteSpeed, 1.579 URLs no sitemap, robots bloqueia IA crawlers.
- Pendente: decidir público-alvo principal (BR vs global) para estratégia; auditoria completa.
- **Decisão do Van:** público é GLOBAL — quer aproveitar os 4 idiomas.
- **Achado técnico:** versões /pt-br/ /es/ /de/ existem e têm hreflang+canonical corretos, MAS o sitemap (1.579 URLs) só lista a versão EN — variantes de idioma não estão no sitemap. Potencial: ~6.300 URLs (4 idiomas). Correção: adicionar variantes ao sitemap.
- **Missão 1 (titles/metas 4 idiomas) concluída e verificada 01/08:** home, liga e H2H traduzidos em PT/ES/DE, EN intacto. Feito pelo antigravidade (via git), validado por mim na origem.
- **Incidente 01/08 ~18:23:** logs do projeto recriados como root (não é cron agendado — manutenção manual/painel; journal mostra só ruído SSH + node naquele segundo). Resultado: gunicorn não subia (Django LOGGING handler 'error_file' não conseguia abrir logs/django_errors.log) e crons do www (live_premium, update_live, odds etc.) não executavam (redirect falha → comando aborta). Fix: `chown www:www logs/*.log` + restart do statsfut.service. Cron live_premium voltou (mtime atualiza). update_live volta no tick 20:25.
- **Pendências:** sitemap multilíngue (P0), JSON-LD (P0), limpeza repo (temp_test.py + video_maker_openclaw.zip), token GitHub no remote (segurança), proteção contra logs root de novo (oferecer cron chown).

## Fim do dia (20:46)
- **Tudo aplicado e validado:** sitemap hreflang (1.572 URLs), JSON-LD, lazy loading, preconnect, 404 customizada. Reenviado sitemap no GSC (Van já tinha propriedade).
- **Mapa de keywords** criado: `seo-keyword-map.md` (4 mercados, estimativas; refinar com GSC).
- **Achado novo:** H1s genéricos ("Overview"/"Visão Geral") nas páginas de liga/H2H; match sem H1 → **Missão 7 pendente** (salva em `proximos-passos.md`).
- Van cansado; retomada amanhã. Pendências do Van: números GSC (páginas válidas, desempenho, queries) + responder sobre blog (post_to_blogger interno ou externo).
- **Ambiente:** host do assistente = mail.statsfut.com (servidor de e-mail), NÃO é o servidor web. Código do site NÃO está aqui (procurei /var/www, /home, /srv, /opt). Projeto é local do Van (git). Regra de ouro: eu entrego artefatos prontos (listas, XML, snippets) e o Van aplica no projeto dele — nunca edito o código do site diretamente (evita divergência de git).
