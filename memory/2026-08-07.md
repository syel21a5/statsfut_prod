
## 07:30 — Diagnóstico "All models are temporarily rate-limited" (Telegram)
- **Causa raiz:** sessão `agent:main:telegram:direct:7883491565` estava PINADA em `gemini-2/gemini-3.5-flash` (modelOverrideSource=user). Pin de usuário é ESTRITO → não usa fallback. Chave gemini-2 (vision2) estourou cota free do Gemini (429, limite 20 req). Resultado: qualquer msg no Telegram → "All models are temporarily rate-limited", mesmo com SambaNova/DeepSeek/Groq/OpenRouter OK.
- **Fix aplicado:** `openclaw models fallbacks add` → fallbacks do agente main agora: deepseek/deepseek-v4-flash, openrouter/deepseek/deepseek-chat, gemini-1/gemini-3.5-flash (removi sambanova da lista pois já é primary). Backup config: openclaw.json.bak-fallback-20260807.
- **Falta:** Van mandar `/model default` (ou `/model gemini-1/gemini-3.5-flash`) no Telegram p/ limpar pin. RPC sessions.patch exige operator.admin (device local não tem). Editar sessions.json à mão com gateway rodando = risco de sobrescrita.
- **Lição:** pins de sessão são estritos e NÃO caem em fallback. Chaves Gemini free estouram fácil (limit 20). Verificar pins antigos quando aparecer esse erro.
